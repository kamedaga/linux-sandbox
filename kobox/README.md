# kobox-specific source

This directory is reserved for code with no corresponding upstream Linux
location, including the sandbox entry point, self-loader, host contract,
feature manifests, and conformance fixtures.

Foundation gates: [POSIX host](host/posix/README.md),
[Linux memory](memory/README.md), and [Linux task/SMP](task/README.md).
